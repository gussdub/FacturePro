{
  "name": "facturepro-backend",
  "version": "2.0.0",
  "description": "FacturePro Backend API - Production Ready",
  "main": "server.py",
  "scripts": {
    "start": "uvicorn server:app --host 0.0.0.0 --port $PORT",
    "dev": "uvicorn server:app --reload --host 0.0.0.0 --port 8000"
  },
  "dependencies": {},
  "engines": {
    "python": "3.11"
  }
}